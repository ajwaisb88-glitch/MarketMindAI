"""Vendored Monster engine — 60-Agents + the 100-point confluence score.

Ported from monster-outlook so the whole system ships in one app. Elliott Wave
is deliberately excluded from this score (it stays a separate engine).
"""
